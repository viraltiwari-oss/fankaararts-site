# Astro fingerprints these filenames with a content hash, so the file at a given
# URL never changes. Cache it for a year — fixes PageSpeed "serve static assets with an
# efficient cache policy" and makes repeat visits load instantly from cache.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
